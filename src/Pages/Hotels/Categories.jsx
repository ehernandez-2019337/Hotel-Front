import React from 'react'
import { Navbar } from '../../components/navbar/Navbar'

export const Categories = ({categories, getCategories}) => {
  console.log(categories)
  return (
    <div>
      <Navbar></Navbar>
    </div>
  )
}

